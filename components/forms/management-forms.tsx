"use client";
import {useActionState}from"react";import{createCohort,inviteUser,linkCohortMember,saveAssignment,saveLearningGoal,updateUserRole}from"@/app/actions/management";import{Button}from"@/components/ui/button";
const Msg=({state}:{state:{error?:string;success?:string}})=>state.error?<p className="rounded-xl bg-red-50 p-3 text-sm font-bold text-red-800" role="alert">{state.error}</p>:state.success?<p className="rounded-xl bg-emerald-50 p-3 text-sm font-bold text-emerald-900" role="status">{state.success}</p>:null;
export function AssignmentForm({assignment,cohorts,goals}:{assignment?:any;cohorts:any[];goals:any[]}){const[state,action,pending]=useActionState(saveAssignment,{});return <form action={action} className="space-y-4"><input type="hidden" name="id" value={assignment?.id??""}/><div><label className="mb-1 block text-sm font-bold">Titel</label><input className="field" name="title" defaultValue={assignment?.title} required maxLength={160}/></div><div><label className="mb-1 block text-sm font-bold">Beschrijving</label><textarea className="field min-h-24" name="description" defaultValue={assignment?.description} maxLength={2000}/></div><div className="grid gap-3 sm:grid-cols-2"><label className="text-sm font-bold">Deadline<input className="field mt-1" type="datetime-local" name="deadline" defaultValue={assignment?.deadline?.slice(0,16)}/></label><label className="text-sm font-bold">Periode<input className="field mt-1" name="period" defaultValue={assignment?.period} maxLength={80}/></label></div><label className="flex items-center gap-2 text-sm font-bold"><input className="size-4 accent-teal-700" type="checkbox" name="published" defaultChecked={assignment?.is_published}/>Zichtbaar voor gekoppelde klassen</label><fieldset><legend className="text-sm font-bold">Klassen</legend><div className="mt-2 grid gap-2 sm:grid-cols-2">{cohorts.map(c=><label className="flex items-center gap-2 text-sm" key={c.id}><input type="checkbox" name="cohortIds" value={c.id} className="size-4 accent-teal-700" defaultChecked={assignment?.cohortIds?.includes(c.id)}/>{c.name}</label>)}</div></fieldset><fieldset><legend className="text-sm font-bold">Leerdoelen</legend><div className="mt-2 max-h-48 space-y-2 overflow-y-auto rounded-xl border border-slate-200 p-3">{goals.map(g=><label className="flex items-start gap-2 text-sm" key={g.id}><input type="checkbox" name="goalIds" value={g.id} className="mt-0.5 size-4 accent-teal-700" defaultChecked={assignment?.goalIds?.includes(g.id)}/>{g.title}</label>)}</div></fieldset><Msg state={state}/><Button disabled={pending}>{pending?"Opslaan…":assignment?"Wijzigingen opslaan":"Opdracht aanmaken"}</Button></form>}
export function CohortForm(){const[state,action,pending]=useActionState(createCohort,{});return <form action={action} className="space-y-3"><label className="block text-sm font-bold">Naam<input className="field mt-1" name="name" required placeholder="Marketing & Communicatie 4C"/></label><label className="block text-sm font-bold">Schooljaar<input className="field mt-1" name="schoolYear" required defaultValue="2026-2027"/></label><Msg state={state}/><Button disabled={pending}>{pending?"Aanmaken…":"Klas aanmaken"}</Button></form>}
export function CohortLinkForm({profiles,cohorts,kind}:{profiles:any[];cohorts:any[];kind:"student"|"teacher"}){const[state,action,pending]=useActionState(linkCohortMember,{});return <form action={action} className="space-y-3"><input type="hidden" name="kind" value={kind}/><label className="block text-sm font-bold">{kind==="student"?"Student":"Docent"}<select className="field mt-1" name="profileId" required><option value="">Kies</option>{profiles.map(p=><option key={p.id} value={p.id}>{p.full_name}</option>)}</select></label><label className="block text-sm font-bold">Klas<select className="field mt-1" name="cohortId" required><option value="">Kies</option>{cohorts.map(c=><option key={c.id} value={c.id}>{c.name}</option>)}</select></label><Msg state={state}/><Button disabled={pending}>{pending?"Koppelen…":"Aan klas koppelen"}</Button></form>}
export function LearningGoalForm({goal}:{goal?:any}){const[state,action,pending]=useActionState(saveLearningGoal,{});return <form action={action} className="space-y-3"><input type="hidden" name="id" value={goal?.id??""}/><label className="block text-sm font-bold">Leerdoel<textarea className="field mt-1 min-h-20" name="title" defaultValue={goal?.title} required maxLength={240}/></label><label className="block text-sm font-bold">Beschrijving<textarea className="field mt-1 min-h-20" name="description" defaultValue={goal?.description} maxLength={2000}/></label><div className="grid gap-3 sm:grid-cols-2"><label className="text-sm font-bold">Domein<input className="field mt-1" name="domain" defaultValue={goal?.domain}/></label><label className="text-sm font-bold">Niveau/kader<input className="field mt-1" name="level" defaultValue={goal?.level}/></label><label className="text-sm font-bold">Kerntaak<input className="field mt-1" name="kerntaak" defaultValue={goal?.kerntaak}/></label><label className="text-sm font-bold">Werkproces<input className="field mt-1" name="werkproces" defaultValue={goal?.werkproces}/></label><label className="text-sm font-bold">Volgorde<input className="field mt-1" type="number" min="0" max="999" name="sortOrder" defaultValue={goal?.sort_order??0}/></label></div><Msg state={state}/><Button disabled={pending}>{pending?"Opslaan…":goal?"Wijzigingen opslaan":"Leerdoel aanmaken"}</Button></form>}
export function UserRoleForm({profile}:{profile:{id:string;role:"student"|"teacher"}}){const[state,action,pending]=useActionState(updateUserRole,{});return <form action={action} className="flex flex-wrap items-center gap-2"><input type="hidden" name="profileId" value={profile.id}/><select className="field min-w-36 flex-1" aria-label="Gebruikersrol" name="role" defaultValue={profile.role}><option value="student">Student</option><option value="teacher">Docent</option></select><Button disabled={pending}>{pending?"Opslaan…":"Rol opslaan"}</Button><Msg state={state}/></form>}
export function InviteUserForm({cohorts}:{cohorts:{id:string;name:string}[]}){const[state,action,pending]=useActionState(inviteUser,{});return <form action={action} className="space-y-4"><label className="block text-sm font-bold">Naam<input className="field mt-1" name="fullName" required/></label><label className="block text-sm font-bold">E-mailadres<input className="field mt-1" type="email" name="email" required/></label><div className="grid gap-3 sm:grid-cols-2"><label className="text-sm font-bold">Rol<select className="field mt-1" name="role"><option value="student">Student</option><option value="teacher">Docent</option></select></label><label className="text-sm font-bold">Klas<select className="field mt-1" name="cohortId"><option value="">Later koppelen</option>{cohorts.map(c=><option key={c.id} value={c.id}>{c.name}</option>)}</select></label></div><Msg state={state}/><Button disabled={pending}>{pending?"Versturen…":"Uitnodiging versturen"}</Button></form>}
