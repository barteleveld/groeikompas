"use client";
import { useState } from "react";
import { BookOpenCheck, GraduationCap, School, Target } from "lucide-react";
import { PageHeader } from "@/components/layout/page-header";
import { useDemo } from "@/components/demo/demo-state";

export default function AdminDemo(){const{state,addUser}=useDemo();const[message,setMessage]=useState("");const cards=[{n:state.users.filter(u=>u.role==="Student").length+8,label:"studenten",icon:GraduationCap},{n:state.users.filter(u=>u.role==="Docent").length+1,label:"docenten",icon:School},{n:6,label:"opdrachten",icon:BookOpenCheck},{n:8,label:"leerdoelen",icon:Target}];return <><PageHeader eyebrow="Beheer" title="Opleiding inrichten" description="Nodig gebruikers uit en houd klassen, modules en leerdoelen overzichtelijk."/><section className="grid gap-4 sm:grid-cols-2 lg:grid-cols-4">{cards.map(({n,label,icon:Icon})=><article className="card p-5" key={label}><div className="flex justify-between"><div><p className="text-3xl font-black">{n}</p><p className="font-bold text-slate-600">{label}</p></div><Icon className="text-teal-700"/></div></article>)}</section><div className="mt-8 grid gap-6 lg:grid-cols-[.8fr_1.2fr]"><form className="card h-fit space-y-4 p-5" onSubmit={e=>{e.preventDefault();const f=new FormData(e.currentTarget);const name=String(f.get("name"));const role=String(f.get("role"));addUser(name,role);setMessage(`${name} is toegevoegd aan de demo.`);e.currentTarget.reset()}}><h2 className="text-xl font-black">Gebruiker uitnodigen</h2><label className="block text-sm font-bold">Naam<input className="field mt-1" name="name" required/></label><label className="block text-sm font-bold">E-mailadres<input className="field mt-1" name="email" type="email" required/></label><label className="block text-sm font-bold">Rol<select className="field mt-1" name="role"><option>Student</option><option>Docent</option></select></label><label className="block text-sm font-bold">Klas<select className="field mt-1"><option>Marketing & Communicatie 4A</option><option>Marketing & Communicatie 4B</option></select></label>{message&&<p className="rounded-xl bg-emerald-50 p-3 text-sm font-bold text-emerald-900">{message}</p>}<button className="rounded-xl bg-teal-700 px-4 py-2.5 font-bold text-white">Uitnodiging versturen</button></form><section><h2 className="mb-4 text-xl font-black">Gebruikers</h2><div className="space-y-3">{state.users.map(u=><article className="card flex items-center justify-between p-4" key={u.id}><p className="font-black">{u.name}</p><span className="rounded-full bg-teal-50 px-2 py-1 text-xs font-bold text-teal-900">{u.role}</span></article>)}</div><h2 className="mb-4 mt-7 text-xl font-black">Modules</h2><div className="grid gap-3 sm:grid-cols-2">{state.modules.map(m=><article className="card p-4" key={m.id}><p className="font-black">{m.title}</p><p className="mt-1 text-sm text-slate-600">{m.period} · {m.assignments.length} opdrachten</p></article>)}</div></section></div></>}
