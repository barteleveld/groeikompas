"use client";
import { useState } from "react";
import { PageHeader } from "@/components/layout/page-header";
import { useDemo } from "@/components/demo/demo-state";

const students=["Lina Bakker","Sem de Jong","Yara Smit","Omar Aydin","Fleur Visser"];
export default function TeacherDemo(){const{state,addModule,addMoment,setStudentStatus}=useDemo();const[tab,setTab]=useState<"overzicht"|"modules"|"planning">("overzicht");const[selected,setSelected]=useState<string|null>(null);return <><PageHeader eyebrow="Docentoverzicht" title="Wie heeft nu wat nodig?" description="Probeer alles uit. Je wijzigingen blijven bewaard op deze computer."/><div className="mb-6 flex flex-wrap gap-2">{[["overzicht","Voortgang"],["modules","Modules"],["planning","Feedback plannen"]].map(([id,label])=><button key={id} onClick={()=>setTab(id as any)} className={`rounded-xl px-4 py-2.5 text-sm font-bold ${tab===id?"bg-teal-700 text-white":"border border-slate-300 bg-white"}`}>{label}</button>)}</div>{tab==="overzicht"&&<div className="grid gap-6 lg:grid-cols-[1.2fr_.8fr]"><div className="overflow-x-auto rounded-2xl border border-slate-200 bg-white"><table className="w-full min-w-[650px] text-left text-sm"><thead className="bg-slate-50"><tr><th className="p-4">Student</th><th className="p-4">Signaal</th><th className="p-4">Actie</th></tr></thead><tbody className="divide-y divide-slate-200">{students.map(name=><tr key={name}><td className="p-4 font-black">{name}</td><td className="p-4"><span className="rounded-full bg-amber-50 px-2 py-1 text-xs font-bold text-amber-950">{state.studentStatus[name]}</span></td><td className="p-4"><button onClick={()=>setSelected(name)} className="font-bold text-teal-700 underline">Bijwerken</button></td></tr>)}</tbody></table></div>{selected?<section className="card h-fit p-5"><h2 className="text-xl font-black">{selected}</h2><p className="mt-1 text-sm text-slate-600">Kies de volgende stap voor deze student.</p><div className="mt-4 grid gap-2">{["Wacht op feedback","Feedback verwerken","Op schema","Afgerond"].map(status=><button onClick={()=>{setStudentStatus(selected,status);setSelected(null)}} className="rounded-xl border border-slate-200 p-3 text-left text-sm font-bold hover:bg-slate-50" key={status}>{status}</button>)}</div></section>:<section className="card h-fit p-5"><h2 className="font-black">Snelle aandacht</h2><p className="mt-2 text-sm text-slate-600">Klik bij een student op ‘Bijwerken’ om de status direct aan te passen.</p></section>}</div>}{tab==="modules"&&<div className="grid gap-6 lg:grid-cols-[.8fr_1.2fr]"><form className="card h-fit space-y-4 p-5" onSubmit={e=>{e.preventDefault();const f=new FormData(e.currentTarget);addModule({title:String(f.get("title")),description:String(f.get("description")),period:String(f.get("period")),assignments:String(f.get("assignments")).split(",").map(x=>x.trim()).filter(Boolean)});e.currentTarget.reset()}}><h2 className="text-xl font-black">Nieuwe module</h2><label className="block text-sm font-bold">Titel<input className="field mt-1" name="title" required/></label><label className="block text-sm font-bold">Beschrijving<textarea className="field mt-1" name="description"/></label><label className="block text-sm font-bold">Periode<input className="field mt-1" name="period" placeholder="Periode 3"/></label><label className="block text-sm font-bold">Opdrachten <span className="font-normal text-slate-500">(gescheiden door komma’s)</span><input className="field mt-1" name="assignments"/></label><button className="rounded-xl bg-teal-700 px-4 py-2.5 font-bold text-white">Module aanmaken</button></form><section><h2 className="mb-4 text-xl font-black">Modules</h2><div className="space-y-3">{state.modules.map(m=><article className="card p-5" key={m.id}><p className="text-xs font-bold uppercase text-teal-700">{m.period||"Doorlopend"}</p><h3 className="mt-1 font-black">{m.title}</h3><p className="mt-1 text-sm text-slate-600">{m.description}</p><p className="mt-3 text-sm font-bold">{m.assignments.length} opdrachten</p></article>)}</div></section></div>}{tab==="planning"&&<div className="grid gap-6 lg:grid-cols-[.8fr_1.2fr]"><form className="card h-fit space-y-4 p-5" onSubmit={e=>{e.preventDefault();const f=new FormData(e.currentTarget);addMoment({title:String(f.get("title")),date:String(f.get("date")),kind:String(f.get("kind")),cohort:"Marketing 4A"});e.currentTarget.reset()}}><h2 className="text-xl font-black">Feedbackmoment plannen</h2><label className="block text-sm font-bold">Titel<input className="field mt-1" name="title" required/></label><label className="block text-sm font-bold">Datum en tijd<input className="field mt-1" type="datetime-local" name="date" required/></label><label className="block text-sm font-bold">Soort<select className="field mt-1" name="kind"><option>Snelle check</option><option>Feedback van docent</option><option>Feedback van medestudent</option><option>Voortgangsgesprek</option></select></label><button className="rounded-xl bg-teal-700 px-4 py-2.5 font-bold text-white">Moment plannen</button></form><section><h2 className="mb-4 text-xl font-black">Planning</h2><div className="space-y-3">{state.moments.map(m=><article className="card p-5" key={m.id}><p className="text-xs font-bold uppercase text-indigo-700">{m.kind} · {m.cohort}</p><h3 className="mt-1 font-black">{m.title}</h3><p className="mt-2 text-sm text-slate-600">{m.date}</p></article>)}</div></section></div>}</>}
