import { InviteUserForm, UserRoleForm } from "@/components/forms/management-forms";
import { PageHeader } from "@/components/layout/page-header";
import { requireRole } from "@/lib/auth/session";
import { createClient } from "@/lib/supabase/server";
export default async function UsersPage(){await requireRole("admin");const supabase=await createClient();const[{data:profiles},{data:cohorts}]=await Promise.all([supabase.from("profiles").select("id,full_name,role").order("full_name"),supabase.from("cohorts").select("id,name").order("name")]);return <><PageHeader eyebrow="Mensen en klassen" title="Gebruikers" description="Nodig studenten en docenten uit, geef ze de juiste rol en koppel ze direct aan een klas."/><div className="grid gap-7 lg:grid-cols-[.8fr_1.2fr]"><section className="card h-fit p-5"><h2 className="mb-4 text-xl font-black">Nieuwe gebruiker uitnodigen</h2><InviteUserForm cohorts={cohorts??[]}/></section><section><h2 className="mb-4 text-xl font-black">Bestaande gebruikers</h2><div className="grid gap-3 sm:grid-cols-2">{(profiles??[]).filter((p:any)=>p.role!=="admin").map((p:any)=><article className="card p-4" key={p.id}><p className="mb-3 font-black">{p.full_name}</p><UserRoleForm profile={p}/></article>)}</div></section></div></>}
