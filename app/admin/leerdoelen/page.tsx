import{LearningGoalForm}from"@/components/forms/management-forms";import{PageHeader}from"@/components/layout/page-header";import{requireRole}from"@/lib/auth/session";import{createClient}from"@/lib/supabase/server";
export default async function GoalManagement(){await requireRole("admin");const supabase=await createClient();const{data:goals}=await supabase.from("learning_goals").select("*").order("domain").order("sort_order");return <><PageHeader eyebrow="Leerdoelenbeheer" title="Leerdoelen ordenen" description="Orden op domein, kerntaak en werkproces. Beheersingsniveaus blijven vast en worden per student bijgehouden."/><div className="grid gap-7 xl:grid-cols-[.8fr_1.2fr]"><section className="card h-fit p-5"><h2 className="mb-4 text-xl font-black">Nieuw leerdoel</h2><LearningGoalForm/></section><section><h2 className="mb-4 text-xl font-black">Bestaande leerdoelen</h2><div className="space-y-4">{(goals??[]).map((g:any)=><details className="card p-5" key={g.id}><summary className="cursor-pointer list-none"><p className="text-xs font-bold uppercase tracking-wide text-teal-700">{g.domain||"Zonder domein"} · {g.kerntaak||"—"} · {g.werkproces||"—"}</p><h3 className="mt-2 font-black">{g.title}</h3></summary><div className="mt-5 border-t border-slate-200 pt-5"><LearningGoalForm goal={g}/></div></details>)}</div></section></div></>}
